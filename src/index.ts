import { handler as addslashes } from './commands/addslashes';import { handler as decode } from './commands/decode';import { handler as decodeUrl } from './commands/decode-url';import { handler as encode } from './commands/encode';import { handler as encodeUrl } from './commands/encode-url';import { handler as encrypt } from './commands/encrypt';import { handler as jsonPretty } from './commands/json-pretty';import { handler as stripslashes } from './commands/stripslashes';export = {  addslashes,  decode,  decodeUrl,  encode,  encodeUrl,  encrypt,  jsonPretty,  stripslashes,}